[build]
  # Run the build script
  base = "website"
  command = "bash build.sh"
  publish = "../dist"
  
[build.environment]
  # Python version
  PYTHON_VERSION = "3.11"
  
# No catch-all redirect needed for MPA - Netlify will serve static files directly
